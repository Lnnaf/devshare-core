type User {
    id: Int
    username: String
    displayName: String
    dateJoin: String
    birthday: String
    email: String
    roleType: String
}

extend type Query {
    getUserById(id: ID): User
}
